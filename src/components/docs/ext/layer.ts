import type { AnyExtension } from "@tiptap/core";

// The page editor's layer extensions (SPEC.md §29). extensions.ts spreads
// this list into the editor; this area's work adds its extensions here.
export const layerExtensions: AnyExtension[] = [];
