"use client";

import type { DocsAreaProps } from "@/components/docs/areas/types";

// The Unitos layer's own controls on the page editor (SPEC.md §29): what
// the reader's tools need that lives inside the editor.
export function UnitosLayer(props: DocsAreaProps) {
  void props;
  return null;
}
