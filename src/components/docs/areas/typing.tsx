"use client";

import type { DocsAreaProps } from "@/components/docs/areas/types";

// The typing area (SPEC.md §29): find and replace, the shortcuts help, and
// the other windows that serve typing.
export function TypingLayer(props: DocsAreaProps) {
  void props;
  return null;
}
