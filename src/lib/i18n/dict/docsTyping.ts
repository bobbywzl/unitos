// UI strings of the page editor's typing area: find and replace, the shortcuts help, spelling
// (SPEC.md §29). zh glossary: dict/common.ts.

const en = {
  placeholder: "",
};

const zh: Record<keyof typeof en, string> = {
  placeholder: "",
};

export const docsTyping = { en, zh } as const;
