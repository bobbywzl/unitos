// UI strings of the page editor's insert area: the @ menu, the right-click menus, tables, images, chips
// (SPEC.md §29). zh glossary: dict/common.ts.

const en = {
  placeholder: "",
};

const zh: Record<keyof typeof en, string> = {
  placeholder: "",
};

export const docsInsert = { en, zh } as const;
