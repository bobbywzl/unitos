// UI strings of the page editor's Unitos layer on the page editor
// (SPEC.md §29). zh glossary: dict/common.ts.

const en = {
  placeholder: "",
};

const zh: Record<keyof typeof en, string> = {
  placeholder: "",
};

export const docsLayer = { en, zh } as const;
