services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dissect
    ports:
      - "5432:5432"
    volumes:
      - dissect-pgdata:/var/lib/postgresql/data

volumes:
  dissect-pgdata:
