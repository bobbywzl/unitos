-- VISUALIZE derivation (SPEC.md §20, Unitos Ultra): the selection as a
-- picture, stored as an SVG ImageAsset and one annotation on the selection.
ALTER TYPE "DerivationType" ADD VALUE IF NOT EXISTS 'VISUALIZE';
